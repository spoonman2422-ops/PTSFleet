rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow reads and writes on all files if the user is authenticated
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
